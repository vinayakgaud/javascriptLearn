//we can use `match` to match the regex pattern
//use `search` to search to search single match
//use `replace` to replace the match
const regex = /bob/g //to create regex we first use two forward slashes andanything under it is pattern
//if we use /bob/ it will only find bob once
//we have to add flag
//g - global search for all matches

//refer to this - https://developer.mozilla.org/en-US/docs/Web/JavaScript/Guide/Regular_expressions

//regex cheat sheet - https://fireship.io/lessons/regex-cheat-sheet-js/